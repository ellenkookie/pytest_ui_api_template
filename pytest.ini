[pytest]
testpaths = tests
python_files = *test*.py
python_classes = *Test*
python_functions = *test*
addopts = --alluredir=allure-files

markers =
    ui: UI tests (user interface)
    api: API tests (application programming interface)
    smoke: Smoke tests
    regression: Regression tests